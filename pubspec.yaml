name: guidik
description: "Guidik MVP - Bloc 0 (Flutter + Provider + go_router + Hive + UUID local, RTL Arabe/Darija)"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

# ───────────────────────────────────────────────────────────────────────────
# Dépendances AUTORISÉES uniquement (voir contrat Bloc 0) :
#   flutter, provider, go_router, hive, hive_flutter, uuid
# Toute autre dépendance doit être validée explicitement avant ajout.
# ───────────────────────────────────────────────────────────────────────────
dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  go_router: ^14.2.7
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
